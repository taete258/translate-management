info:
  name: create-key
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/api/projects/:id/keys"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
  body:
    mode: json
    json: |
      {
        "key": "common.button.save",
        "description": "Save button label"
      }
  params:
    - name: id
      value: "1"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
