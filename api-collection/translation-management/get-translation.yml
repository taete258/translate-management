info:
  name: get-translation
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/export/test-project/th?format=json"
  headers:
    - name: X-API-Key
      value: "{{API_KEY}}"
  params:
    - name: format
      value: json
      type: query

runtime:
  variables:
    - name: slug
      value: test-project

settings:
  encodeUrl: true
  timeout: 60
  followRedirects: true
  maxRedirects: 5
