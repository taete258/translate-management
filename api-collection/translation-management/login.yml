info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "username":"ratchanon.p",
        "password": "091772"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
